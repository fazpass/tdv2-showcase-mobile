
class NotifiableDevice {
  final String id;
  final String name;
  final String osVersion;
  final String series;
  final String cpu;

  NotifiableDevice(this.id, this.name, this.osVersion, this.series, this.cpu);
}